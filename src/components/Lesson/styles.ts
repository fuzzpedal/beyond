import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    lessonTimeText: {
        fontWeight: 'bold',
        marginTop: 20,
        marginBottom: 10,
    },

    nameText: {
        padding: 4,
        fontSize: 16,
    },
});